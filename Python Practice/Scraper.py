from urllib.request import urlopen
from bs4 import BeautifulSoup


def scrape(bsObj):
    for element in html:
        print(bsObj)
    


def main():
    html = input(urlopen(scraping))
    bsObj = BeautifulSoup(html.read(element))
    choice == "y"
    while choice == "y":
        scraping = input("input a website url to scrape: ")
        element = input("input a site element you want to search for: ")


if __name__ == "__main__":
    main()


