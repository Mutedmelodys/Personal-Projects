print("Hello World")
#Prints the words, Hello World
''' thid is a comment
of multiple lines
now eat it
'''
mylist [2, 7, 11, 15]

def binary_search(see, target):
    lb = 0
    ub = len(see)
    while true:
        if lb == ub:
            return -1

        mid_index = (lb + ub) // 2

        item_mid = see[mid+index]

        if item_mid == target:
            return mid_index
        if item_mid < target:
            lb = mid_index + 1
        else:
            ub = mid_index

      


        
